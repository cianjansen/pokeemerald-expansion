# Project Spec

## What this is

A personal Pokémon Emerald romhack built on top of [pokeemerald-expansion](https://github.com/rh-hideout/pokeemerald-expansion) (pret decomp + community expansion). Custom content is being added incrementally, largely by Claude Code agent sessions. This file exists so a fresh agent session has enough context to keep working without re-discovering the whole project from scratch.

Custom region so far: **Ouderkerk** (Dutch-themed), maps prefixed `data/maps/Ouderkerk*`. Content and NPC dialog for this hack are written in Dutch, often as in-jokes for a specific friend group.

**This project is worked on from multiple machines** (as of 2026-08-16: a Mac and a Windows/WSL2 machine), with git used to sync between them. Don't assume the toolchain/OS described in a past session's notes still matches the one you're running in now — check "Toolchain" below for the current session's actual platform, and verify commands work before trusting stale platform-specific paths. **Also `git pull` before starting work** — this file and the maps below get edited from both machines, and stale local state has caused real lost-work incidents this project (see Status).

## Current world state (as of 2026-08-16)

A fresh session should read this before touching maps — it's the map graph plus what's real vs. still a stub, so you don't have to reconstruct it from `git log`.

- **New Game** → skips the Birch intro entirely, spawns the player (male, "FRITS") directly in `Ouderkerk_MargrietHouse_2F` (Margriet's bedroom, NPC there). Downstairs (`Ouderkerk_MargrietHouse_1F`) has the starter-picking pokéballs (Shinx/Riolu/Piplup) and the exit door.
- `Ouderkerk_MargrietHouse_1F` exit → **`OuderkerAmstel`** (outdoor town map, replaces the old placeholder `Ouderkerk` map from the very first session — that original `MAP_OUDERKERK` still exists in `data/maps/Ouderkerk/` but is no longer connected to anything reachable from a fresh save; don't confuse the two). `OuderkerAmstel` has:
  - The bike-blocker checkpoint (NPC + 4 coord_event triggers, north side) gating progress until `FLAG_GOT_FATBIKE` is set — shared logic in `Ouderkerk/scripts.inc`'s `Common_EventScript_BikeBlocker*`, reused via thin per-map wrapper scripts (see Conventions below for the pattern).
  - Two house doors: **Jonas' house** (`Ouderker_JonasHouse_1F`, 2 roaming NPCs — Glacia/Michel-ish joke lines) and **Cas' house** (`Ouderker_CasHouse_1F`, 4 NPCs: Karin, Anton (gives 5 Super Potions on first talk), 2 dogs).
  - Connects further out to **`Ouderkerkerplas`** (right) and **`CMroute1`** (up).
- **`Ouderkerkerplas`**: has wild encounters (land + water/fishing, see Status), two trainer battles (`RIJKE_PENSIONADO` fisherman, `SARA`), a talkable NPC (`JEFFREY`), and **`JONAS` himself standing here — but he's still just a placeholder object (`script: NULL`)**. He's the one meant to hand out the Fatbike / set `FLAG_GOT_FATBIKE` (per the bike-blocker's own dialog: "ik zag Jonas daar op een enge Fatbike rondfietsen"), but that script doesn't exist yet — **right now `FLAG_GOT_FATBIKE` is never set anywhere in the game**, so the bike-blocker checkpoint is currently unbeatable. This is the most obvious next task.
- **`CMroute1`**: completely empty stub — zero object/warp/coord/bg events, default layout tiles only. Connected but unbuilt.
- Map-name popup / Town Map section: `OuderkerAmstel`, the three Margriet house maps, `Ouderker_CasHouse_1F`, and `Ouderker_JonasHouse_1F` all have real names now (see `MAPSEC_OUDERKER_*` in Status). **`Ouderkerkerplas` and `CMroute1` still show "ABANDONED SHIP"** (`MAPSEC_ABANDONED_SHIP` placeholder) — not yet given their own section.

## Repo layout notes

- This is a standard pret-style decomp. Source of truth for maps is `data/maps/<MapName>/map.json` and `data/layouts/layouts.json`.
- `header.inc`, `events.inc`, and `connections.inc` inside each map folder are **auto-generated from map.json and are gitignored** — never hand-edit them, edit `map.json` instead.
- To regenerate a map's `.inc` files after editing its `map.json` (without doing a full `make`):
  ```
  ./tools/mapjson/mapjson map emerald data/maps/<MapName>/map.json data/layouts/layouts.json data/maps/<MapName>
  ```
- To sanity-check a map/script change compiles without doing a full ROM build (the full `make` may be broken locally due to an unrelated missing-newlib-headers toolchain issue — see "Known environment issues" below):
  ```
  make build/emerald/data/map_events.o
  ```
  This runs the real preproc → cpp → as pipeline over all `scripts.inc`/`map.json`-derived data and will fail loudly on bad script/text syntax.

## Conventions used in this hack

- **NPC scripts**: standard pret macros — `lock`, `faceplayer`, `msgbox ..., MSGBOX_DEFAULT`, `goto_if_set`, `setflag`, `release`, `end`. Look at existing `data/maps/*/scripts.inc` files for the idiom.
- **"Talk once, then different dialog forever after" pattern**:
  ```
  EventScript_Foo::
      lock
      faceplayer
      goto_if_set FLAG_X, EventScript_FooRepeat
      msgbox Text_FooFirst, MSGBOX_DEFAULT
      setflag FLAG_X
      release
      end

  EventScript_FooRepeat::
      msgbox Text_FooRepeat, MSGBOX_DEFAULT
      release
      end
  ```
- **Flags for this kind of one-off story/dialog state**: reserved from the `FLAG_UNUSED_0x0xx` pool in `include/constants/flags.h` — rename one of those defines in place (don't delete the surrounding block, don't reuse a flag that's already meaningful elsewhere). Give it a descriptive name (e.g. `FLAG_TALKED_TO_MARGRIET`).
- **Object event placement**: `"flag": "0"` on an object_event means it's always visible (no hide-flag). Use `elevation` matching the metatile it stands on (check `data/layouts/<Layout>/map.bin` / the layout's tile grid if unsure — don't guess).
- **Dialog text**: `.string` macro. `\n` = line break within the same textbox, `\l` = further line within the same (3-line) box, `\p` = new textbox page (player presses A), `$` = end of string. Keep each line under ~38 characters. `charmap.txt` supports Dutch accented characters (`é`, `ë`, `ï`, etc.) if needed. In-game proper nouns/names are conventionally written in caps (e.g. `MARGRIET`, `POKéMON`).
- **Picking an `OBJ_EVENT_GFX_*` sprite for a new NPC — check it's actually usable on Emerald first.** This is an Emerald build (`IS_FRLG` false), but the graphics data for FRLG/Kanto-exclusive character sprites (e.g. `OBJ_EVENT_GFX_CLERK`) still compiles in unconditionally — no `#if IS_FRLG` guard, so nothing stops you from placing one, and the struct itself (OAM size, palette tag, pic table) looks completely normal on inspection. It still crashes the game on map load (confirmed by bisection: `OBJ_EVENT_GFX_CLERK` alone in an otherwise-empty room reproduced it 100% of the time; swapping to `OBJ_EVENT_GFX_WOMAN_2` fixed it instantly, same map, same everything else). The exact mechanism was never fully root-caused — comparing the graphics struct field-by-field against known-good sprites showed nothing — so treat any FRLG-exclusive sprite as unsafe by inference, not just the ones caught so far. **Before using a graphics ID, run:**
  ```
  grep -rl '"<OBJ_EVENT_GFX_NAME>"' data/maps/*/map.json | grep -v _Frlg
  ```
  If that comes back empty, the sprite has never been used on a non-Kanto map in this whole codebase — don't use it; pick something with real Emerald-side precedent instead (e.g. grep shows plenty of hits for generic `OBJ_EVENT_GFX_WOMAN_*`/`MAN_*`/`BOY_*`/`GIRL_*`).

### Cloning an existing map as a starting point for a new area

Porymap's "duplicate map" (and hand-copying a `map.json`) copies every symbolic name as-is. Names aren't auto-uniquified, so a cloned map silently keeps referencing/sharing the original's identity in a few places. Go through all of these on the clone before treating it as done — none of them cause a build error if skipped, which is exactly why they're easy to miss:

- **`local_id`** on object_events: becomes a `#define LOCALID_X <n>` in the auto-generated `include/constants/map_event_ids.h`. It's a plain macro, not scoped to the map, so if two maps reuse the same `local_id` name with *different* numeric positions, whichever is defined later in that generated header silently wins everywhere — including in the *original* map's own scripts. This showed up for real: `Ouderkerk` (cloned from `LittlerootTown`) and `LOCALID_LITTLEROOT_RIVAL`/`LOCALID_LITTLEROOT_BIRCH` colliding, breaking LittlerootTown's own intro script. Fixed by renaming the clone's `local_id`s to be map-specific (`LOCALID_OUDERKERK_*`). **Tell:** `-Wall -Werror` build logs a `'LOCALID_X' redefined` warning (see "Toolchain" for how to actually get a build that surfaces this). **Before renaming a colliding one, `grep -rn` the name repo-wide, not just the map's own `scripts.inc`** — a *shared* script (e.g. `data/scripts/players_house.inc`) can intentionally hardcode one `local_id` name used by multiple maps by design (matching indices on purpose, e.g. two gender-variant house maps). Renaming away from under that isn't a warning, it's a hard assembler error (`non-constant expression in ".if" statement`, because the now-undefined symbol isn't a number anymore). If you hit that, the fix is usually to have each map's own entry-point script pass its correct `local_id` into the shared script via a scratch var (`VAR_0x8008` or similar) instead of the shared script hardcoding a name — see the `LOCALID_PLAYERS_HOUSE_2F_MOM` fix in Status below for a worked example.
- **`flag`** on object_events (the per-object visibility/hide flag): this is a real save-file flag, and reusing the original map's flag name means the two objects share hidden/shown state — toggle one, the other changes too. **No build warning at all for this one.** `Ouderkerk`'s Fat Man/Mom currently still share `FLAG_HIDE_LITTLEROOT_TOWN_FAT_MAN`/`FLAG_HIDE_LITTLEROOT_TOWN_MOM_OUTSIDE` with LittlerootTown's — known, not yet fixed as of 2026-08-15 (see Status below). Give the clone's objects their own flags (reserve fresh ones the same way as the `FLAG_TALKED_TO_MARGRIET` example above).
- **`script`** references on object_events/bg_events/coord_events: cloning doesn't fork the script — the clone's objects keep calling the *original* map's `scripts.inc` symbols (e.g. `Ouderkerk`'s Twin/Fat Man/Mom/sign objects still call `LittlerootTown_EventScript_*`), which means the clone shows the original's text/logic verbatim (still says "LITTLEROOT", may reference the original map-specific vars/state) until you give the clone its own scripts and point these at them.
- Lower priority / only matters if the area needs to feel distinct: `region_map_section` (controls the Town Map / Fly list name), `music`.

## Toolchain

**This project is actively built from two machines, not a one-way migration** — a Mac and a Windows/WSL2 machine, both still in regular use as of 2026-08-16 (an earlier version of this section framed the Mac setup as "superseded"; that was wrong — a full multi-hour session's worth of work, including the FRLG-sprite crash hunt above, happened successfully on the Mac *after* that note was written). Figure out which machine you're actually on and use the matching subsection below; don't assume from a past session's notes.

### Windows + WSL2 (Ubuntu)

- The canonical repo clone lives inside the WSL2 Linux filesystem at `~/decomps/pokeemerald-expansion` (i.e. `\\wsl.localhost\Ubuntu\root\decomps\pokeemerald-expansion` from Windows) — **not** under `/mnt/c/...` — because building against the Windows-mounted filesystem from WSL2 is drastically slower.
- Build toolchain is plain Ubuntu apt packages (no devkitPro needed on this platform): `build-essential binutils-arm-none-eabi gcc-arm-none-eabi libnewlib-arm-none-eabi git libpng-dev python3`, already installed in the `Ubuntu` WSL distro. `wsl -d Ubuntu` defaults to the `root` user (set via `/etc/wsl.conf`, `[user]\ndefault=root`) — the distro's interactive first-run "create a UNIX user" wizard can't be driven non-interactively, so this sidesteps it.
- To build from Windows PowerShell:
  ```
  wsl -d Ubuntu -- bash -c "cd ~/decomps/pokeemerald-expansion && make -j$(nproc)"
  ```
  or just from inside a WSL shell: `cd ~/decomps/pokeemerald-expansion && make -j$(nproc)` (16 cores available on this machine).
- **`reload.ps1`** (repo root) is the Windows equivalent of the old Mac `./reload`: runs the WSL build, then launches Windows-native `mGBA.exe` (installed via `winget install JeffreyPfau.mGBA`, at `C:\Program Files\mGBA\mGBA.exe`) pointed at the ROM via its WSL UNC path, then prints `git status --short`. Run it from Windows PowerShell:
  ```
  powershell -File \\wsl.localhost\Ubuntu\root\decomps\pokeemerald-expansion\reload.ps1
  ```
  Unlike the old Mac script, this one does **not** have the Porymap no-op-rewrite-discarding step (see Mac notes below) — not yet ported, so watch `git status` after Porymap saves for reorder-only noise on this platform.
  A clean build has **zero** warnings — treat any `redefined` warning in the log as a real bug to fix (see the map-cloning checklist above), not noise to ignore.
- For a quick syntax check of just map/script data without a full build (works from either a WSL shell or via `wsl -d Ubuntu -- bash -c "..."`):
  ```
  make build/emerald/data/map_events.o
  ```
- No emulator CLI is set up for automated in-game verification — `mGBA.exe` is GUI-only. `reload.ps1` opens it with the new build automatically; actually playing through a change still means clicking through by hand.
- The `tools/mapjson/mapjson event_constants` mode (regenerates `include/constants/map_event_ids.h` from every map.json) breaks/errors when passed the full ~943-file list as separate args — don't invoke it directly for that; a full `make`/`reload.ps1` regenerates it correctly (it's gitignored, auto-generated, never commit it). If you need to hand-verify a `local_id` rename without a full build, it's safe to edit that generated header directly for local testing.

### Mac

- The `arm-none-eabi-gcc` on `PATH` (Homebrew's) is missing newlib (`string.h`, `alloca.h`, etc.) and **cannot build this project** — a plain `make` fails immediately with `fatal error: string.h: No such file or directory`. Don't try to fix that install; just use the working one below.
- A working toolchain **is** installed at `/opt/devkitpro/devkitARM/bin` (includes newlib). Prepend it to `PATH` (and export `DEVKITARM`/`DEVKITPRO`) for the build:
  ```
  PATH="/opt/devkitpro/devkitARM/bin:$PATH" DEVKITARM="/opt/devkitpro/devkitARM" DEVKITPRO="/opt/devkitpro" make -j"$(sysctl -n hw.ncpu)"
  ```
- `./reload` (repo root) is the edit-build-play loop: it exports `DEVKITARM`/`DEVKITPRO` correctly, discards Porymap no-op rewrite noise from `git status`, runs a full `make -j`, then quits/relaunches `mGBA.app` with the freshly built `pokeemerald.gba`.
- **For crash debugging**, launch mGBA directly with logging instead of `./reload`, so you get a persistent log file to grep instead of just watching it happen once:
  ```
  nohup /Applications/mGBA.app/Contents/MacOS/mGBA -C logToFile=1 -C "logFile=/path/to/mgba.log" -l 511 "$(pwd)/pokeemerald.gba" > /dev/null 2>&1 &
  ```
  A real crash shows up as a `[GAME ERROR] GBA Memory: Bad memory Load8: 0x189b189d`-style block right after a `DMA 3 0x03007DA8 -> 0x0600xxxx (8100:0800)` VRAM-clear sequence in the log (that clear is the normal per-map-load screen wipe, not itself a bug — it's what runs *right before* the crash, every time). This is exactly how the FRLG-sprite bug above got bisected: strip a map down to zero objects, confirm it stops crashing, then add objects back one at a time until it reproduces again.

## Status / Latest work

_Only add an entry here for major/config-level changes — new systems, toolchain/workflow changes, or a bugfix whose lesson generalizes. Routine content work (maps, objects, scripts, dialog) doesn't need an entry. Newest entry on top — don't delete older entries, this is the changelog a new agent session should skim first._

- **2026-08-16**: **Major bug hunt: `OBJ_EVENT_GFX_CLERK`/`OBJ_EVENT_GFX_MR_FUJI`/`OBJ_EVENT_GFX_FISHER` are FRLG-exclusive sprites that crash the game on Emerald.** Placing an NPC with one of these graphics IDs on `Ouderker_CasHouse_1F` (Clerk/MrFuji) or `Ouderkerkerplas` (Fisher) hard-crashed on map load — CPU jumps to executing garbage from a near-zero address, then either freezes or soft-resets to the title screen. This took a long, largely unproductive debugging session to pin down: elevation mismatches, `movement_type`, `map_type`/`music` placeholders, `region_map_sections.json` array sizing, and the dogs (`OBJ_EVENT_GFX_POOCHYENA`) were all suspected and ruled out one at a time by bisection (remove all objects → no crash; add back one at a time → `OBJ_EVENT_GFX_CLERK` alone reproduced it 100%). The graphics struct itself looks completely normal (right OAM size, right palette tag shape, right pic table) — comparing it field-by-field against known-good sprites showed nothing, so **the actual mechanism was never fully root-caused.** What *is* confirmed: these three graphics IDs are used *exclusively* in `_Frlg`-suffixed (Kanto) maps everywhere else in this codebase, never in a plain Emerald map — and swapping to a sprite with real non-FRLG precedent (`OBJ_EVENT_GFX_WOMAN_2`, `OBJ_EVENT_GFX_MAN_3`, `OBJ_EVENT_GFX_FISHERMAN` respectively) fixed each crash instantly, no other changes. Codified as a standing rule in the Conventions section above (grep `data/maps/*/map.json` excluding `_Frlg` before using any `OBJ_EVENT_GFX_*` you haven't seen used on a real Emerald map) — **apply that check up front for any new NPC graphic**, don't wait to hit this again. If you do hit an identical-looking crash (VRAM clear → immediate garbage BIOS reads, `Bad memory Load8: 0x189b189d` in mGBA's log) on a map you're adding NPCs to, suspect the graphics ID first now.
- **2026-08-16**: Fixed `trainerbattle_single` scripts leaving the player stuck after a rematch attempt with an already-defeated trainer (`Ouderkerkerplas`'s `RIJKE_PENSIONADO`/`SARA`). Root cause + the required fix (`msgbox DefeatText, MSGBOX_AUTOCLOSE` — must be `AUTOCLOSE` not `DEFAULT`, since `trainerbattle_single` implicitly locks the player and only `Std_MsgboxAutoclose` releases it) is now a standing rule in `CLAUDE.md` — read it before writing any trainer battle script, it's load-bearing, not a style choice.
- **2026-08-16**: Finished `Ouderker_JonasHouse_1F` (2 roaming NPCs, talkable) and `Ouderker_CasHouse_1F` (Karin, Anton — gives 5 Super Potions on first talk via `FLAG_TALKED_TO_ANTON` — and 2 dogs) end to end: both houses' door warps connected to `OuderkerAmstel`'s two previously-placeholder self-referential warps (now `MAP_OUDERKER_JONAS_HOUSE_1F`/`MAP_OUDERKER_CAS_HOUSE_1F`), all NPCs scripted, all sprite/movement-type footguns from this same session fixed. The `OuderkerAmstel` bike-blocker (see "Current world state" above) was refactored to share its dialog/push-back logic across maps instead of hardcoding one map's `local_id` — `Common_EventScript_BikeBlocker*` in `Ouderkerk/scripts.inc`, called via thin per-map wrapper scripts that `setvar VAR_0x8008, LOCALID_X` then `goto` in (same pattern as the `LOCALID_PLAYERS_HOUSE_2F_MOM` fix in an earlier entry) — add a matching wrapper pair for any future bike-blocker checkpoint rather than duplicating the dialog.
- **2026-08-16**: Added real Town Map / map-name-popup sections instead of the shared `MAPSEC_ABANDONED_SHIP` placeholder, for `OuderkerAmstel` (→ "OUDERKERK"), all three `Ouderkerk_MargrietHouse_*` maps (→ "MARGRIET'S HOUSE", shared one section), `Ouderker_CasHouse_1F` (→ "CAS' HOUSE"), `Ouderker_JonasHouse_1F` (→ "JONAS' HOUSE"). New entries added to `src/data/region_map/region_map_sections.json` (auto-generates both `include/constants/region_map_sections.h` and the name table — no other file needs hand-editing) with no `x`/`y`/`width`/`height` fields, matching the existing pattern for sections that don't need a physical Town Map position (e.g. `MAPSEC_SECRET_BASE`, `MAPSEC_INSIDE_OF_TRUCK`). `Ouderkerkerplas` and `CMroute1` still show "ABANDONED SHIP" — not yet done.
- **2026-08-16**: A round of uncommitted local work on this Mac session got silently reverted mid-session (probably a `git pull`/sync from the Windows machine landing on top of uncommitted changes) — the `local_id` prefix and script/movement-type wiring on `OuderkerAmstel`'s bike-blocker NPC disappeared and had to be redone. **Lesson: commit working changes promptly when bouncing between machines**, don't leave a working tree dirty across a sync — CLAUDE.md's spec-update rule and this multi-machine setup make it easy to forget mid-task.
- **2026-08-16**: Further Porymap scaffolding on the new outdoor maps (still work-in-progress, not yet scripted). `OuderkerAmstel`: added a placeholder `OBJ_EVENT_GFX_GYM_GUY` object event (no script/flag set, `script: NULL`), a second warp event `(14,17)` back to `MAP_OUDERKER_AMSTEL` itself (self-referential — likely unfinished; original `(7,15)` warp to `Ouderkerk_MargrietHouse_1F` moved to `(5,17)`), and 5 `coord_event` triggers along `y=-3` (all placeholder `script: NULL`, writing to scratch `VAR_TEMP_0` with value `0`) — **note two of these five are exact duplicates** at `(11,-3)`, likely an accidental double-place in Porymap, not yet deduplicated. `Ouderkerkerplas`: added a placeholder NPC object event named `JONAS` (`OBJ_EVENT_GFX_BIKER`, no script). None of this is wired to real scripts/flags yet — pure map-layout scaffolding, committed as-is per user request; needs scripting + the duplicate coord_event cleanup before it does anything in-game.

- **2026-08-16**: Added three new outdoor `CMTowns`-group maps via Porymap, extending the world out from Margriet's House: `OuderkerAmstel` (`MAP_OUDERKER_AMSTEL`/`LAYOUT_OUDERKER_AMSTEL`), `Ouderkerkerplas` (`MAP_OUDERKERKERPLAS`/`LAYOUT_OUDERKERKERPLAS`), and `CMroute1` (`MAP_CMROUTE1`/`LAYOUT_CMROUTE1`) — all currently empty layouts (no object/warp/coord/bg events except where noted) with `region_map_section: MAPSEC_ABANDONED_SHIP` (placeholder, not yet given a real Town Map section) and `music: MUS_DUMMY` (placeholder). Connections: `Ouderkerk_MargrietHouse_1F`'s ground-floor exit warp was rewired from `MAP_OUDERKERK` to `MAP_OUDERKER_AMSTEL` (with a warp back at `(7,15)` on `OuderkerAmstel`), and `OuderkerAmstel` connects `right`→`Ouderkerkerplas` and `up`→`CMroute1`. **All three are still blank/undecorated stubs** — no tiles placed beyond default layout, no NPCs, no encounters, no real map name/music — this is scaffolding for the region layout, not finished content. Also registered all three in `data/maps/map_groups.json` (`gMapGroup_CMTowns`) and `data/layouts/layouts.json`, and added their `scripts.inc` includes to `data/event_scripts.s`. Not yet build-verified on this Windows/WSL2 setup as of this commit — run `reload.ps1` to confirm before adding content on top.
- **2026-08-16**: Migrated dev environment from Mac to a clean Windows machine. Installed `gh` CLI (winget), authenticated as `cianjansen`, cloned the repo twice: once to `C:\Users\Administrator\Documents\pokeemerald-expansion` (Windows filesystem, via `gh repo clone` — kept only as a secondary/incidental copy, **not** used for building), and once to `~/decomps/pokeemerald-expansion` inside WSL2 Ubuntu (via plain `git clone`, no `gh` auth needed since the repo is public) — **the WSL2 copy is canonical for building**, per WSL2 performance guidance in the project's own `INSTALL.md`/`docs/install/windows/WSL.md`. Set up WSL2 + Ubuntu (enabling `Microsoft-Windows-Subsystem-Linux`/`VirtualMachinePlatform` Windows features required a reboot); Ubuntu's interactive first-run user-creation wizard can't be driven non-interactively, worked around by setting `default=root` in `/etc/wsl.conf` after a `wsl --shutdown`+forced-kill of the hung OOBE process. Installed apt build deps (`build-essential binutils-arm-none-eabi gcc-arm-none-eabi libnewlib-arm-none-eabi git libpng-dev python3`) and Windows-native `mGBA` (via `winget install JeffreyPfau.mGBA`). Verified a full clean `make -j16` ROM build succeeds (zero warnings, ~3m50s) and that the resulting `pokeemerald.gba` actually loads and runs in `mGBA.exe`. Added `reload.ps1` (repo root) as the Windows/WSL2 equivalent of the old Mac `./reload` — see "Toolchain" above; **note it does not yet port the Mac script's Porymap no-op-rewrite `git status` cleanup step**, that's a known gap if it's needed on this platform. Full rewrite of the "Toolchain" section above to document this; old Mac-specific content preserved under a new "Historical: Mac" subheading rather than deleted.
- **2026-08-15**: Added a starter-picking scene to replace the skipped Birch intro. Three `OBJ_EVENT_GFX_ITEM_BALL` objects in `Ouderkerk_MargrietHouse_1F` (Shinx/Riolu/Piplup, `Ouderkerk_MargrietHouse_1F_EventScript_{Shinx,Riolu,Piplup}Ball` in that map's `scripts.inc`) ask (yes/no) then `givemon ..., 5` on yes; picking one hides/removes all three (shared `FLAG_HIDE_MARGRIET_HOUSE_1F_STARTER_BALLS`) and sets `FLAG_GOT_MARGRIET_STARTER`. Also sets `FLAG_SYS_POKEMON_GET` here — that's the flag `src/start_menu.c` checks to show the POKéMON option in the pause menu; vanilla sets it on Route 101 right after Birch's lab, which our skip-intro flow never passes through, so it had to move here or the menu option would never appear (this resolves the "no starter Pokémon" gap noted in the entry below). Also fixed a bug where `Ouderkerk_MargrietHouse_1F`'s upstairs warp still pointed at vanilla `LittlerootTown_MaysHouse_2F` instead of `Ouderkerk_MargrietHouse_2F` (made the Margriet NPC from the first session unreachable). **Map-cloning footgun encountered again, differently:** fixed a `LOCALID_PLAYERS_HOUSE_2F_MOM` collision between `LittlerootTown_BrendansHouse_2F`/`MaysHouse_2F` (introduced by trimming `MaysHouse_2F`'s object list, shifting its indices) — but unlike the earlier Ouderkerk case, a simple rename broke the build (hard assembler error, not just a warning), because `data/scripts/players_house.inc`'s shared "mom comes upstairs" cutscene hardcodes that one symbol and is called from both maps. Fixed by keeping the local_ids unique per-map but threading the correct one in via `VAR_0x8008`, set by each map's own `*_EventScript_WallClock` entry point before it jumps into the shared script, instead of the shared script hardcoding a name that has to mean two different things at once. Lesson for the map-cloning checklist above: before renaming a colliding `local_id`, grep for it repo-wide (not just the map's own `scripts.inc`) — shared/common scripts can depend on two maps' local_ids matching by design.
- **2026-08-15**: Added a "skip the intro" path for New Game. Selecting New Game from the title screen no longer runs the Professor Birch speech or the naming/gender-select screens — it hardcodes the player to male, name "FRITS", and warps straight to `HEAL_LOCATION_OUDERKERK_MARGRIET_HOUSE_2F`. Implementation: `src/main_menu.c`'s `ACTION_NEW_GAME` case (in `Task_HandleMainMenuAPressed`) now sets `gSaveBlock2Ptr->playerGender`/`playerName` directly and jumps to a new `CB2_NewGameSkipIntro` (added in `src/overworld.c`, declared in `include/overworld.h`) instead of starting the `Task_NewGameBirchSpeech_Init` task chain; `CB2_NewGameSkipIntro` mirrors `CB2_NewGame` but re-warps to the heal location (via `SetWarpDestinationToHealLocation` + `WarpIntoMap`) right after `NewGameInitData()` instead of using the truck-intro `gFieldCallback`. The old Birch speech task chain is left in place but is intentionally unreachable — `Task_NewGameBirchSpeech_Init` is still assigned to a task (then that task is immediately destroyed) purely so it stays *referenced*, since this build uses `-Werror -Wall` and an actually-orphaned static function would fail the build. **Known gap:** since the Birch scene is fully skipped, the player currently starts with an empty party (no starter Pokémon) — no replacement starter-grant logic has been added; flag this to the user if it comes up. Verified by a full `PATH=/opt/devkitpro/devkitARM/bin:$PATH make` ROM build succeeding cleanly; not yet verified by actually playing through New Game in an emulator (see "Toolchain" above for why that's manual).
